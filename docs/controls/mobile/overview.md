# 移动端控件文档入口

当前解决方案中没有 `AtomUI.Mobile.Controls` 项目，移动端控件文档暂不展开。

保留本目录的原因：

- `AtomUI.Native` 已对 `AtomUI.Mobile.Controls` 开放内部可见性。
- 后续如果增加移动端控件包，控件使用文档可以直接进入 `docs/controls/mobile/`。
- 移动端模块架构文档应放入 `docs/modules/mobile-controls/`，具体控件文档仍放在本目录。

