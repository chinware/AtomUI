<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">

    <ControlTheme x:Key="{x:Type atom:ColorSliderThumb}" TargetType="atom:ColorSliderThumb">
        <Setter Property="Template">
            <ControlTemplate>
                <atom:PixelAlignedBorder Name="PART_Frame"
                                         Background="{atom:ColorPickerTokenSharedTokenResource ColorBgElevated}"
                                         BorderBrush="{TemplateBinding BorderBrush}"
                                         BorderThickness="{TemplateBinding BorderThickness}"
                                         Padding="2"
                                         CornerRadius="{TemplateBinding CornerRadius}">
                    <atom:PixelAlignedBorder Name="PART_InnerEllipse"
                                             BorderBrush="{atom:ColorPickerTokenSharedTokenResource ColorFillSecondary}"
                                             CornerRadius="{TemplateBinding InnerCornerRadius}"
                                             BorderThickness="{TemplateBinding BorderThickness}"
                                             Background="{TemplateBinding ColorValueBrush}">
                    </atom:PixelAlignedBorder>
                </atom:PixelAlignedBorder>
            </ControlTemplate>
        </Setter>

        <Setter Property="ClipToBounds" Value="False" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="BorderBrush" Value="{atom:ColorPickerTokenSharedTokenResource ColorFillSecondary}" />
        <Setter Property="BorderThickness" Value="{atom:ColorPickerTokenSharedTokenResource BorderThickness}" />
        
        <Setter Property="Width"
                Value="{atom:ColorPickerTokenResource ColorPickerSliderThumbSize}" />
        <Setter Property="Height"
                Value="{atom:ColorPickerTokenResource ColorPickerSliderThumbSize}" />
        
        <Style Selector="^:focus,^:pressed">
            <Setter Property="BorderBrush" Value="{atom:ColorPickerTokenSharedTokenResource ColorPrimaryActive}" />
        </Style>
    </ControlTheme>

</ResourceDictionary>
