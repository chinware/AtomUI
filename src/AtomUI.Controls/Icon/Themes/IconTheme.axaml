<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:Icon}" TargetType="atom:Icon">
        
        <Setter Property="Template">
            <ControlTemplate>
                <Border Background="{TemplateBinding Background}"
                        Height="{TemplateBinding Height}"
                        Width="{TemplateBinding Width}"/>
            </ControlTemplate>
        </Setter>
        
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="{atom:IconTokenSharedTokenResource IconSize}" />
        <Setter Property="Width" Value="{atom:IconTokenSharedTokenResource IconSize}" />
        <Setter Property="FillBrush" Value="{atom:IconTokenSharedTokenResource ColorIconHover}" />
        <Setter Property="StrokeBrush" Value="{atom:IconTokenSharedTokenResource ColorIconHover}" />
        <Setter Property="StrokeWidth" Value="{atom:IconTokenResource StrokeWidth}" />
        <Setter Property="StrokeLineCap" Value="{atom:IconTokenResource StrokeLineCap}" />
        <Setter Property="StrokeLineJoin" Value="{atom:IconTokenResource StrokeLineJoin}" />
        
        <Style Selector="^[IconTheme=TwoTone]">
            <Setter Property="StrokeBrush" Value="{atom:IconTokenSharedTokenResource ColorPrimary}" />
            <Setter Property="FillBrush" Value="{atom:IconTokenSharedTokenResource ColorInfoBg}" />
        </Style>
        
        <Style Selector="^[IconTheme=MultiColor]">
            <Setter Property="StrokeBrush" Value="{atom:IconTokenSharedTokenResource ColorIconHover}" />
            <Setter Property="FillBrush" Value="{atom:IconTokenSharedTokenResource ColorPrimary}" />
        </Style>
        
        <Setter Property="SecondaryFillBrush" Value="{atom:IconTokenResource SecondaryFillColor}" />
        <Setter Property="SecondaryStrokeBrush" Value="{atom:IconTokenResource SecondaryStrokeColor}" />
        <Setter Property="FallbackBrush" Value="White" />

    </ControlTheme>
</ResourceDictionary>
