<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:DefaultTourIndicator}" TargetType="atom:DefaultTourIndicator">

        <Setter Property="ItemSpacing" Value="{atom:TourTokenSharedTokenResource SpacingXXS}" />
        <Setter Property="IndicatorSize" Value="{atom:TourTokenResource IndicatorSize}" />
        <Setter Property="IndicatorColor" Value="{atom:TourTokenSharedTokenResource ColorFill}" />
        <Setter Property="IndicatorActiveColor"
                Value="{atom:TourTokenSharedTokenResource ColorPrimary}" />
        
        <Style Selector="^[StyleType=Primary]">
            <Setter Property="IndicatorColor"
                    Value="{atom:TourTokenResource PrimaryPrevBtnBg}" />
            <Setter Property="IndicatorActiveColor"
                    Value="{atom:TourTokenSharedTokenResource ColorTextLightSolid}" />
        </Style>
    </ControlTheme>
</ResourceDictionary>