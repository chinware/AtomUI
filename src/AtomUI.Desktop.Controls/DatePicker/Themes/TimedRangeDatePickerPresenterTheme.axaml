<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    xmlns:themes="using:AtomUI.Desktop.Controls.Themes"
    xmlns:calendarview="using:AtomUI.Desktop.Controls.CalendarView"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:TimedRangeDatePickerPresenter}"
                  TargetType="atom:TimedRangeDatePickerPresenter">
        <ControlTheme.BasedOn>
            <themes:DatePickerPresenterTheme TargetType="atom:TimedRangeDatePickerPresenter" />
        </ControlTheme.BasedOn>
        <Setter Property="Template">
            <ControlTemplate>
                <DockPanel Name="RootLayout"
                           LastChildFill="True">
                    <Border Name="ButtonsFrame"
                            BorderThickness="{TemplateBinding BorderThickness, Converter={x:Static atom:DatePickerPresenterTheme.BorderThicknessFilterConverter}}"
                            IsVisible="{TemplateBinding IsButtonsPanelVisible}"
                            DockPanel.Dock="Bottom">
                        <Panel Name="ButtonsLayout">
                            <atom:Button
                                Name="PART_NowButton"
                                ButtonType="Link"
                                HorizontalAlignment="Left"
                                SizeType="Small"
                                Content="{atom:DatePickerLangResource Now}" />
                            <atom:Button
                                Name="PART_TodayButton"
                                ButtonType="Link"
                                HorizontalAlignment="Left"
                                SizeType="Small"
                                Content="{atom:DatePickerLangResource Today}" />
                            <atom:Button
                                Name="PART_ConfirmButton"
                                ButtonType="Primary"
                                HorizontalAlignment="Right"
                                SizeType="Small"
                                Content="{atom:CommonLangResource Ok}" />
                        </Panel>
                    </Border>
                    <StackPanel Orientation="Horizontal">
                        <calendarview:RangeCalendar Name="PART_CalendarView"
                                                    VerticalAlignment="Top"
                                                    IsMotionEnabled="{TemplateBinding IsMotionEnabled}"
                                                    SelectedDate="{TemplateBinding SelectedDateTime}"
                                                    SecondarySelectedDate="{TemplateBinding SecondarySelectedDateTime}"/>
                        <atom:TimeView
                            Name="PART_TimeView"
                            VerticalAlignment="Top"
                            IsMotionEnabled="{TemplateBinding IsMotionEnabled}" 
                            ClockIdentifier="{TemplateBinding ClockIdentifier}"
                            IsVisible="{TemplateBinding IsShowTime}"/>
                        
                    </StackPanel>
                </DockPanel>
            </ControlTemplate>
        </Setter>
    </ControlTheme>
</ResourceDictionary>