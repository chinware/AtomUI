<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:primitives="using:AtomUI.Desktop.Controls.Primitives"
    xmlns:atom="https://atomui.net"
    xmlns:themeResources="using:AtomUI.Theme.Resources"
    xmlns:generated="using:AtomUI.Generated.AtomUI_Desktop_Controls"
    themeResources:ControlTokenScope.Identity="{x:Static generated:IndicatorScrollViewerThemeAsset.Identity}">
    <ControlTheme x:Key="{x:Type primitives:IndicatorScrollBarThumb}"
                  TargetType="primitives:IndicatorScrollBarThumb">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="Frame"
                        Background="{TemplateBinding Background}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                    <Border.Transitions>
                        <Transitions>
                            <atom:SolidColorBrushTransition Property="Background" Easing="LinearEasing" Duration="{atom:SharedTokenResource MotionDurationMid}"/>
                        </Transitions>
                    </Border.Transitions>
                </Border>
            </ControlTemplate>
        </Setter>
                
        <Setter Property="Background" Value="{atom:IndicatorScrollViewerTokenResource ThumbBg}"/>
        <Setter Property="CornerRadius" Value="{atom:IndicatorScrollViewerTokenResource ThumbCornerRadius}"/>
        <Setter Property="MinHeight"
                Value="{atom:IndicatorScrollViewerTokenResource ScrollBarThickness}" />
        <Setter Property="Width" Value="{atom:IndicatorScrollViewerTokenResource ThumbThickness}" />
    </ControlTheme>
</ResourceDictionary>