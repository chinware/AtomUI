<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:CountBadgeAdorner}" TargetType="atom:CountBadgeAdorner">
        <Setter Property="Template">
            <ControlTemplate>
                <atom:MotionActor Name="PART_MotionActor"
                                  Classes.semantic-indicator="True"
                                  ClipToBounds="False">
                    <Panel
                        Name="RootLayout">
                        <Border Name="BadgeIndicator"
                                BoxShadow="{TemplateBinding BoxShadow}"
                                Background="{TemplateBinding BadgeColor}" />
                        <atom:TextBlock
                            Name="BadgeText"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Text="{TemplateBinding CountText}" 
                            Foreground="{TemplateBinding Foreground}"/>
                    </Panel>
                </atom:MotionActor>
            </ControlTemplate>
        </Setter>

        <Setter Property="Foreground" Value="{atom:CountBadgeTokenResource TextColor}" />
        <Setter Property="ClipToBounds" Value="False" />
        <Setter Property="BadgeShadowSize" Value="{atom:CountBadgeTokenResource ShadowSize}" />
        <Setter Property="BadgeShadowColor" Value="{atom:CountBadgeTokenResource ShadowColor}" />
        <Setter Property="MotionDuration" Value="{atom:SharedTokenResource MotionDurationMid}" />
        <Setter Property="BadgeColor" Value="{atom:CountBadgeTokenResource IndicatorColor}" />

        <Style Selector="^[IsAdornerMode=True]">
            <Style Selector="^ /template/ Panel#RootLayout">
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="VerticalAlignment" Value="Top" />
            </Style>
        </Style>
        <Style Selector="^ /template/ Border#BadgeIndicator">
            <Setter Property="Height" Value="{atom:CountBadgeTokenResource IndicatorHeight}" />
            <Setter Property="MinWidth" Value="{atom:CountBadgeTokenResource IndicatorHeight}" />
            <Setter Property="CornerRadius"
                    Value="{atom:CountBadgeTokenResource CornerRadius}" />
        </Style>
        <Style Selector="^ /template/ atom|TextBlock#BadgeText">
            <Setter Property="FontSize" Value="{atom:CountBadgeTokenResource TextFontSize}" />
            <Setter Property="Padding" Value="{atom:CountBadgeTokenResource TextPadding}" />
        </Style>

        <Style Selector="^[Size=Small]">
            <Style Selector="^ /template/ Border#BadgeIndicator">
                <Setter Property="Height" Value="{atom:CountBadgeTokenResource IndicatorHeightSM}" />
                <Setter Property="MinWidth" Value="{atom:CountBadgeTokenResource IndicatorHeightSM}" />
                <Setter Property="CornerRadius"
                        Value="{atom:CountBadgeTokenResource CornerRadiusSM}" />
            </Style>
            <Style Selector="^ /template/ atom|TextBlock#BadgeText">
                <Setter Property="FontSize" Value="{atom:CountBadgeTokenResource TextFontSizeSM}" />
            </Style>
        </Style>
    </ControlTheme>
</ResourceDictionary>
