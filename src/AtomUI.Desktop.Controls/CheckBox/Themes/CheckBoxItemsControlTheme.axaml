<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:CheckBoxItemsControl}" TargetType="atom:CheckBoxItemsControl">
        <Setter Property="Template">
            <ControlTemplate>
                <ItemsPresenter Name="PART_ItemsPresenter"
                                ItemsPanel="{TemplateBinding ItemsPanel}" />
            </ControlTemplate>
        </Setter>

        <Setter Property="ItemsPanel">
            <ItemsPanelTemplate>
                <WrapPanel
                    Orientation="{Binding (atom:CheckBoxItemsControl).Orientation, RelativeSource={RelativeSource TemplatedParent}}"
                    ItemSpacing="{Binding (atom:CheckBoxItemsControl).ItemSpacing, RelativeSource={RelativeSource TemplatedParent}}"
                    LineSpacing="{Binding (atom:CheckBoxItemsControl).LineSpacing, RelativeSource={RelativeSource TemplatedParent}}"/>
            </ItemsPanelTemplate>
        </Setter>

        <Setter Property="ItemSpacing" Value="{atom:SharedTokenResource SpacingXS}" />
        <Setter Property="LineSpacing" Value="{atom:SharedTokenResource SpacingXS}" />
        <Setter Property="ClipToBounds" Value="False" />
        
    </ControlTheme>
</ResourceDictionary>
