<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:TreeSelect}" TargetType="atom:TreeSelect">
        <Setter Property="Template">
            <ControlTemplate>
                <Panel>
                    <atom:TreeSelectAddOnDecoratedBox
                        Name="{x:Static atom:AddOnDecoratedBox.AddOnDecoratedBoxPart}"
                        StyleVariant="{TemplateBinding StyleVariant}"
                        SizeType="{TemplateBinding SizeType}"
                        Status="{TemplateBinding Status}"
                        DataValidationErrors.Errors="{TemplateBinding DataValidationErrors.Errors}"
                        LeftAddOn="{TemplateBinding LeftAddOn}"
                        LeftAddOnTemplate="{TemplateBinding LeftAddOnTemplate}"
                        RightAddOn="{TemplateBinding RightAddOn}"
                        RightAddOnTemplate="{TemplateBinding RightAddOnTemplate}"
                        IsEnabled="{TemplateBinding IsEnabled}"
                        ContentLeftAddOn="{TemplateBinding ContentLeftAddOn}"
                        ContentLeftAddOnTemplate="{TemplateBinding ContentLeftAddOnTemplate}"
                        IsMultiple="{TemplateBinding IsMultiple}"
                        CustomControlHeight="{TemplateBinding Height}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        IsSelectionEmpty="{TemplateBinding IsSelectionEmpty}"
                        IsDropDownOpen="{TemplateBinding IsDropDownOpen}"
                        CompactSpaceItemPosition="{TemplateBinding CompactSpaceItemPosition}"
                        CompactSpaceOrientation="{TemplateBinding CompactSpaceOrientation}"
                        IsUsedInCompactSpace="{TemplateBinding IsUsedInCompactSpace}">
                        <atom:AddOnDecoratedBox.ContentRightAddOn>
                            <StackPanel Orientation="Horizontal"
                                        Spacing="{atom:SharedTokenResource SpacingXS}">
                                <atom:SelectMaxCountIndicator
                                    Name="PART_SelectMaxCountIndicator"
                                    MaxCount="{CompiledBinding $parent[atom:TreeSelect].MaxCount}"
                                    SelectedCount="{CompiledBinding $parent[atom:TreeSelect].SelectedCount}"
                                    IsVisible="{CompiledBinding $parent[atom:TreeSelect].IsShowMaxCountIndicator}" />
                                <ContentPresenter
                                    Name="PART_ContentRightAddOnPresenter"
                                    Content="{CompiledBinding $parent[atom:TreeSelect].ContentRightAddOn}"
                                    ContentTemplate="{CompiledBinding $parent[atom:TreeSelect].ContentRightAddOnTemplate}"
                                    IsVisible="{CompiledBinding $parent[atom:TreeSelect].ContentRightAddOn, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                <atom:SelectHandle
                                    Name="PART_SelectHandle"
                                    FormFeedback="{CompiledBinding $parent[atom:TreeSelect].FormFeedback}"
                                    LoadingIcon="{CompiledBinding $parent[atom:TreeSelect].SuffixLoadingIcon}"
                                    OpenIndicator="{CompiledBinding $parent[atom:TreeSelect].SuffixIcon}"
                                    IsFilterEnabled="{CompiledBinding $parent[atom:TreeSelect].IsFilterEnabled}"
                                    IsEnabled="{CompiledBinding $parent[atom:TreeSelect].IsEnabled}"
                                    IsMotionEnabled="{CompiledBinding $parent[atom:TreeSelect].IsMotionEnabled}"
                                    IsLoading="{CompiledBinding $parent[atom:TreeSelect].IsLoading}"
                                    IsAllowClear="{CompiledBinding $parent[atom:TreeSelect].IsAllowClear}"
                                    IsSelectionEmpty="{CompiledBinding $parent[atom:TreeSelect].IsSelectionEmpty}"
                                    IsDropDownOpen="{CompiledBinding $parent[atom:TreeSelect].IsDropDownOpen}" />
                            </StackPanel>
                        </atom:AddOnDecoratedBox.ContentRightAddOn>
                        <Panel>
                            <atom:TextBlock
                                Name="PlaceholderText"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                Text="{TemplateBinding PlaceholderText}"
                                IsVisible="{TemplateBinding IsPlaceholderTextVisible}"
                                Foreground="{TemplateBinding PlaceholderForeground}" />
                            <atom:SelectFilterTextBox
                                Name="PART_SingleFilterInput"
                                IsVisible="False"
                                VerticalAlignment="Stretch"
                                SizeType="Custom"
                                Padding="0"
                                BorderThickness="0"
                                FontSize="{TemplateBinding FontSize}"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontStyle="{TemplateBinding FontStyle}"
                                FontWeight="{TemplateBinding FontWeight}"
                                IsCustomFontSize="True"
                                PlaceholderForeground="{TemplateBinding PlaceholderForeground}"
                                atom:OverflowTip.IsEnabled="{TemplateBinding IsShowOverflowTip}"
                                atom:OverflowTip.Text="{Binding Text, RelativeSource={RelativeSource Self}}"
                                atom:OverflowTip.ShowDelay="{TemplateBinding OverflowTipDelay}"
                                atom:OverflowTip.Placement="{TemplateBinding OverflowTipPlacement}"
                                atom:OverflowTip.PlacementTarget="{Binding $parent[atom:TreeSelectAddOnDecoratedBox]}" />
                            <atom:SelectTagAwareTextBox Name="SelectedItemsBox"
                                                        HorizontalAlignment="Stretch"
                                                        VerticalAlignment="Center"
                                                        SizeType="{TemplateBinding SizeType}"
                                                        SelectedItems="{TemplateBinding EffectiveSelectedItems}"
                                                        IsFilterEnabled="{TemplateBinding IsFilterEnabled}"
                                                        IsShowOverflowTip="{TemplateBinding IsShowOverflowTip}"
                                                        OverflowTipDelay="{TemplateBinding OverflowTipDelay}"
                                                        OverflowTipPlacement="{TemplateBinding OverflowTipPlacement}"
                                                        IsDropDownOpen="{TemplateBinding IsDropDownOpen}"
                                                        MaxTagCount="{TemplateBinding MaxTagCount}"
                                                        IsResponsiveTagMode="{TemplateBinding IsResponsiveTagMode}"
                                                        CustomControlHeight="{TemplateBinding Height}"
                                                        ContentFramePadding="{CompiledBinding $parent[atom:TreeSelectAddOnDecoratedBox].ContentFramePadding}"
                                                        ContentMinHeight="{CompiledBinding $parent[atom:TreeSelectAddOnDecoratedBox].ContentMinHeight}"
                                                        InputBorderThickness="{CompiledBinding $parent[atom:TreeSelectAddOnDecoratedBox].InnerBoxBorderThickness}"
                                                        IsVisible="False"
                                                        Focusable="True" />
                        </Panel>
                    </atom:TreeSelectAddOnDecoratedBox>
                    <atom:Popup Name="PART_Popup"
                                WindowManagerAddShadowHint="False"
                                IsLightDismissEnabled="True"
                                OverlayDismissEventPassThrough="True"
                                RequestedPlacement="{TemplateBinding PopupPlacement}"
                                ShouldUseOverlayLayer="{TemplateBinding ShouldUseOverlayPopup}"
                                IsOpen="{TemplateBinding IsDropDownOpen, Mode=TwoWay}" />
                </Panel>
            </ControlTemplate>
        </Setter>
        <Setter Property="FilterHighlightForeground"
                Value="{atom:SharedTokenResource ColorPrimary}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="IsMotionEnabled" Value="{atom:SharedTokenResource EnableMotion}" />
        <Setter Property="PopupContentPadding"
                Value="{atom:SelectTokenResource PopupContentPadding}" />
        <Setter Property="ItemHeight" Value="{atom:SharedTokenResource ControlHeight}" />
        <Setter Property="PlaceholderForeground"
                Value="{atom:SharedTokenResource ColorTextPlaceholder}" />

        <Style Selector="^[SizeType=Large]">
            <Setter Property="FontSize" Value="{atom:SharedTokenResource FontSizeLG}" />
        </Style>
        <Style Selector="^[SizeType=Middle], ^[SizeType=Custom]">
            <Setter Property="FontSize" Value="{atom:SharedTokenResource FontSize}" />
        </Style>
        <Style Selector="^[SizeType=Small]">
            <Setter Property="FontSize" Value="{atom:SharedTokenResource FontSize}" />
        </Style>

        <Style Selector="^ /template/ atom|Popup#PART_Popup">
            <Setter Property="MarginToAnchor"
                    Value="{atom:PopupHostTokenResource MarginToAnchor}" />
        </Style>
        <Style Selector="^ /template/ Border#PopupFrame">
            <Setter Property="Background" Value="{atom:SharedTokenResource ColorBgElevated}" />
            <Setter Property="CornerRadius"
                    Value="{atom:PopupHostTokenResource BorderRadius}" />
        </Style>

        <Style Selector="^[IsMultiple=False]">
            <Style Selector="^ /template/ atom|SelectFilterTextBox#PART_SingleFilterInput">
                <Setter Property="IsVisible" Value="True" />
            </Style>
            <Style Selector="^[IsFilterEnabled=False]">
                <Style Selector="^ /template/ atom|TreeSelectAddOnDecoratedBox#PART_AddOnDecoratedBox">
                    <Setter Property="Cursor" Value="Hand" />
                </Style>

            </Style>
        </Style>

        <Style Selector="^[IsMultiple=True]">
            <Style Selector="^ /template/ atom|SelectTagAwareTextBox#SelectedItemsBox">
                <Setter Property="IsVisible" Value="True" />
            </Style>
            <Style Selector="^[IsFilterEnabled=False]">
                <Style Selector="^ /template/ atom|SelectAddOnDecoratedBox#PART_AddOnDecoratedBox">
                    <Setter Property="Cursor" Value="Hand" />
                </Style>
            </Style>
        </Style>

        <Setter Property="ItemTemplate">
            <TreeDataTemplate x:DataType="atom:ITreeItemNode"
                              ItemsSource="{Binding Children}">
                <atom:TextBlock Text="{Binding Header}" />
            </TreeDataTemplate>
        </Setter>

        <Style Selector="^ /template/ Border#PopupFrame">
            <Setter Property="MinWidth"
                    Value="{atom:TreeSelectTokenResource MinPopupWidth}" />
        </Style>

        <Style Selector="^:disabled">
            <Setter Property="Foreground" Value="{atom:SharedTokenResource ColorTextDisabled}" />
        </Style>
    </ControlTheme>
</ResourceDictionary>
