<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:antdicons="https://atomui.net/icons/antdesign"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:UploadTextListItemHeader}" TargetType="atom:UploadTextListItemHeader">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="InfoFrame" CornerRadius="{TemplateBinding CornerRadius}"
                        Background="{TemplateBinding Background}">
                    <DockPanel LastChildFill="True" Height="{TemplateBinding EffectiveLineHeight}">
                        <Panel DockPanel.Dock="Left">
                            <antdicons:PaperClipOutlined Name="NormalIcon" IsVisible="False"/>
                            <antdicons:LoadingOutlined Name="LoadingIcon" IsVisible="False" LoadingAnimation="Spin"/>
                        </Panel>
                        <atom:IconButton Name="DeleteAction"
                                         Icon="{antdicons:AntDesignIconProvider DeleteOutlined}"
                                         DockPanel.Dock="Right"
                                         VerticalAlignment="Stretch"
                                         Width="{TemplateBinding EffectiveLineHeight}"
                                         IsVisible="False"
                                         IsMotionEnabled="{TemplateBinding IsMotionEnabled}" 
                                         Tag="{x:Static atom:UploadListActions.Remove}"/>
                        <TextBlock Name="FileName" Text="{TemplateBinding FileName}" />
                    </DockPanel>
                </Border>
            </ControlTemplate>
            
        </Setter>
        
        <Style Selector="^ /template/ atom|Icon">
            <Setter Property="StrokeBrush" Value="{atom:SharedTokenResource ColorIcon}" />
        </Style>
        <Setter Property="RelativeLineHeight"
                Value="{atom:SharedTokenResource RelativeLineHeight}" />
        <Setter Property="Margin" Value="{atom:UploadTokenResource TextListItemMargin}" />
        <Setter Property="CornerRadius" Value="{atom:SharedTokenResource BorderRadiusSM}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />

        <Style Selector="^ /template/ atom|IconButton">
            <Setter Property="IconBrush" Value="{atom:UploadTokenResource ActionsColor}" />
            <Setter Property="IconWidth" Value="{atom:SharedTokenResource IconSize}" />
            <Setter Property="IconHeight" Value="{atom:SharedTokenResource IconSize}" />
        </Style>
        
        <Style Selector="^[Status=Uploading]">
            <Style Selector="^ /template/ atom|Icon#LoadingIcon">
                <Setter Property="IsVisible" Value="True" />
            </Style>
        </Style>
        <Style Selector="^:not(^[Status=Uploading])">
            <Style Selector="^ /template/ atom|Icon#NormalIcon">
                <Setter Property="IsVisible" Value="True" />
            </Style>
        </Style> 
        
        <Style Selector="^[Status=Failed]">
            <Setter Property="Foreground" Value="{atom:SharedTokenResource ColorError}" />
            <Style Selector="^ /template/ atom|Icon">
                <Setter Property="StrokeBrush" Value="{atom:SharedTokenResource ColorError}" />
            </Style>
            <Style Selector="^ /template/ atom|IconButton">
                <Setter Property="IconBrush" Value="{atom:SharedTokenResource ColorError}" />
            </Style>
            <Setter Property="atom:ToolTip.Tip" Value="{Binding ErrorMessage, RelativeSource={RelativeSource Self}}" />
            <Setter Property="atom:ToolTip.Placement" Value="Top" />
        </Style>

        <Style Selector="^ /template/ TextBlock#FileName">
            <Setter Property="Padding" Value="{atom:UploadTokenResource TextListNamePadding}" />
        </Style>

        <Style Selector="^ /template/ atom|IconButton:pointerover">
            <Setter Property="Background"
                    Value="{atom:SharedTokenResource ControlItemBgHover}" />
        </Style>

        <Style Selector="^:pointerover">
            <Setter Property="Background" Value="{atom:SharedTokenResource ControlItemBgHover}" />

            <Style Selector="^ /template/ atom|IconButton#DeleteAction">
                <Setter Property="IsVisible" Value="True" />
            </Style>
        </Style>

        <Style Selector="^[IsMotionEnabled=True]">
            <Setter Property="Transitions">
                <Transitions>
                    <atom:SolidColorBrushTransition Property="Background" Duration="{atom:SharedTokenResourceValue Kind=MotionDurationMid}" />
                </Transitions>
            </Setter>
        </Style>

    </ControlTheme>
</ResourceDictionary>