<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atom="https://atomui.net"
    x:ClassModifier="internal">
    <ControlTheme x:Key="{x:Type atom:UploadDefaultDropArea}" TargetType="atom:UploadDefaultDropArea">
        <Setter Property="Template">
            <ControlTemplate>
                <atom:DashedBorder Name="Frame"
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="{TemplateBinding BorderBrush}"
                                   BorderThickness="{TemplateBinding BorderThickness}"
                                   CornerRadius="{TemplateBinding CornerRadius}"
                                   Padding="{TemplateBinding Padding}">
                    <StackPanel Orientation="Vertical">
                        <atom:IconPresenter Name="IconPresenter"
                                            Icon="{TemplateBinding DropIcon}" />
                        <ContentPresenter Name="HeaderContentPresenter"
                                          Content="{TemplateBinding Header}"
                                          ContentTemplate="{TemplateBinding HeaderTemplate}" />
                        <ContentPresenter Name="SubHeaderContentPresenter"
                                          Content="{TemplateBinding SubHeader}"
                                          ContentTemplate="{TemplateBinding SubHeaderTemplate}" />
                    </StackPanel>
                </atom:DashedBorder>
            </ControlTemplate>
        </Setter>
        
        <Setter Property="Background" Value="{atom:UploadTokenSharedTokenResource ColorFillAlter}"/>
        <Setter Property="CornerRadius" Value="{atom:UploadTokenSharedTokenResource BorderRadiusLG}"/>
        <Setter Property="BorderBrush" Value="{atom:UploadTokenSharedTokenResource ColorBorder}"/>
        <Setter Property="BorderThickness" Value="{atom:UploadTokenSharedTokenResource BorderThickness}"/>
        <Setter Property="Padding" Value="{atom:UploadTokenSharedTokenResource Padding}"/>
        <Setter Property="Header" Value="{atom:UploadLangResource DragUploadHead}"/>
        <Setter Property="IsMotionEnabled" Value="{atom:UploadTokenSharedTokenResource EnableMotion}"/>
        <Setter Property="DragDrop.AllowDrop" Value="True"/>
        
        <Style Selector="^ /template/ atom|DashedBorder#Frame">
            <Setter Property="StrokeDashArray" Value="4, 2"/>
        </Style>

        <Style Selector="^ /template/ atom|IconPresenter#IconPresenter">
            <Setter Property="IconBrush" Value="{atom:UploadTokenSharedTokenResource ColorPrimary}"/>
            <Setter Property="Width" Value="{atom:UploadTokenResource DragIconSize}"/>
            <Setter Property="Height" Value="{atom:UploadTokenResource DragIconSize}"/>
            <Setter Property="Margin" Value="{atom:UploadTokenResource DragIconMargin}"/>
        </Style>
        
        <Style Selector="^:pointerover">
            <Setter Property="BorderBrush" Value="{atom:UploadTokenSharedTokenResource ColorPrimaryHover}"/>
        </Style>
        
        <Style Selector="^ /template/ ContentPresenter#HeaderContentPresenter">
            <Setter Property="Foreground" Value="{atom:UploadTokenSharedTokenResource ColorTextHeading}"/>
            <Setter Property="FontSize" Value="{atom:UploadTokenSharedTokenResource FontSizeLG}"/>
            <Setter Property="Margin" Value="{atom:UploadTokenResource DragHeaderMargin}"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        
        <Style Selector="^ /template/ ContentPresenter#SubHeaderContentPresenter">
            <Setter Property="Foreground" Value="{atom:UploadTokenSharedTokenResource ColorTextDescription}"/>
            <Setter Property="FontSize" Value="{atom:UploadTokenSharedTokenResource FontSize}"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        
        <Style Selector="^:disabled">
            <Style Selector="^ /template/ atom|IconPresenter#IconPresenter">
                <Setter Property="IconBrush" Value="{atom:UploadTokenSharedTokenResource ColorTextDisabled}"/>
            </Style>

            <Style Selector="^ /template/ ContentPresenter#HeaderContentPresenter">
                <Setter Property="Foreground" Value="{atom:UploadTokenSharedTokenResource ColorTextDisabled}"/>
            </Style>

            <Style Selector="^ /template/ ContentPresenter#SubHeaderContentPresenter">
                <Setter Property="Foreground" Value="{atom:UploadTokenSharedTokenResource ColorTextDisabled}"/>
            </Style>
        </Style>

        <Style Selector="^[IsMotionEnabled=True]">
            <Setter Property="Transitions">
                <Transitions>
                    <atom:SolidColorBrushTransition Property="BorderBrush" Duration="{atom:SharedTokenResourceValue Kind=MotionDurationMid}" />
                </Transitions>
            </Setter>
        </Style>

    </ControlTheme>
</ResourceDictionary>