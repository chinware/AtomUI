<ControlTheme xmlns="https://github.com/avaloniaui"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:atom="https://atomui.net"
              x:Class="AtomUI.Desktop.Controls.Themes.TreeViewFlyoutPresenterTheme"
              TargetType="atom:TreeViewFlyoutPresenter"
              x:ClassModifier="internal">
    <Setter Property="Template">
        <ControlTemplate>
            <atom:ArrowDecoratedBox Name="{x:Static atom:AbstractArrowDecoratedBox.ArrowDecoratorPart}"
                                    IsArrowVisible="{TemplateBinding IsArrowVisible}"
                                    ArrowPosition="{TemplateBinding ArrowPosition}"
                                    BackgroundSizing="{TemplateBinding BackgroundSizing}"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}">
                <ItemsPresenter Name="ItemsPresenter"
                                ItemsPanel="{TemplateBinding ItemsPanel}" />
            </atom:ArrowDecoratedBox>
        </ControlTemplate>
    </Setter>
    
    <Style Selector="^:is(atom|TreeViewFlyoutPresenter)">
        <Setter Property="IsMotionEnabled" Value="{atom:SharedTokenResource EnableMotion}" />
        <Setter Property="CornerRadius" Value="{atom:PopupTokenResource PopupCornerRadius}" />
        <Setter Property="Padding" Value="{atom:TreeViewFlyoutPresenterTokenResource PopupContentPadding}" />
        
        <Style Selector="^ /template/ atom|ArrowDecoratedBox#PART_ArrowDecorator">
            <Setter Property="Background" Value="{atom:TreeViewFlyoutPresenterTokenResource PopupBgColor}" />
            <Setter Property="MinWidth" Value="{atom:TreeViewFlyoutPresenterTokenResource PopupMinWidth}" />
            <Setter Property="MaxWidth" Value="{atom:TreeViewFlyoutPresenterTokenResource PopupMaxWidth}" />
            <Setter Property="MinHeight" Value="{atom:TreeViewFlyoutPresenterTokenResource PopupMinHeight}" />
            <Setter Property="MaxHeight" Value="{atom:TreeViewFlyoutPresenterTokenResource PopupMaxHeight}" />
        </Style>
    </Style>

</ControlTheme>
