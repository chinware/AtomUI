<Project>
    <PropertyGroup>
        <NoWarn>$(NoWarn);CS7035</NoWarn>
        <AvaloniaVersion>11.1.3</AvaloniaVersion>
        <AtomUIVersion>0.0.1</AtomUIVersion>
        <NugetPackageVersion>$(AtomUIVersion)-build-$([System.DateTime]::Now.ToString("yyyyMMdd"))</NugetPackageVersion>
        <FileVersion>$([System.DateTime]::Now.ToString("yyyy.MM.dd HH:mm:ss"))</FileVersion>
        <AssemblyVersion>$(AtomUIVersion)</AssemblyVersion>
    </PropertyGroup>
</Project>